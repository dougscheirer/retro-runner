var editform = $("#edit-task");
if (editform.html() == "") {
    editform.css("width", "75%")
    editform.html("<%= j (render partial: "outstandings/form", locals: {
    title: "Edit task",
    button_title: "Modify",
    action: outstanding_path(@outstanding),
    action_method: "PATCH" }) %>");
    editform.slideDown(400);
}
else {
    editform.slideUp(400, function() {editform.html("");});
}
